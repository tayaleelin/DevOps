name: Deploy AKS
run-name: ${{ github.actor }} AKS Deployment
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Set account to Azure subscription
        uses: azure/set-subscription@v1
        with:
          subscription: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Deploy to AKS
        uses: azure/aks-set-context@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
          resource-group: ${{ secrets.AZURE_RESOURCE_GROUP }}
          cluster-name: ${{ secrets.AZURE_AKS_CLUSTER_NAME }}
      # - name: Install Otomi on AKS
      #   uses: azure/aks-helm@v1
      #   with:
      #     creds: ${{ secrets.AZURE_CREDENTIALS }}
      #     resource-group: ${{ secrets.AZURE_RESOURCE_GROUP }}
      #     cluster-name: ${{ secrets.AZURE_AKS_CLUSTER_NAME }}
      #     chart-name: otomi
      #     chart-version: ${{ secrets.OTOMI_CHART_VERSION }}
